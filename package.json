{
  "name": "meal-planner",
  "version": "1.0.0",
  "description": "A static meal planner generator",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && open dist/index.html",
    "dev": "npm run build && python3 -m http.server 8000 --directory dist"
  },
  "keywords": [
    "meal-planner",
    "static-site-generator",
    "recipes"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
